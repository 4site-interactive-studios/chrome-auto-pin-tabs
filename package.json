{
  "name": "auto-pin-tabs",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Tooling-only package.json: lets Node run the test suites and the build script. Chrome ignores this file and reads manifest.json.",
  "scripts": {
    "test": "node test/matcher.test.mjs && node test/order.test.mjs && node test/cleanup.test.mjs && node test/drift.test.mjs",
    "build": "node build.mjs"
  }
}
